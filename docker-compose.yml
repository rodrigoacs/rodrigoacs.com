version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rodrigoacs-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
    - proxy

networks:
    proxy:
        external: true
